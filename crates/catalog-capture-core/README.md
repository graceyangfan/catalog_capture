# catalog-capture-core

Core capture contracts: config, `CapturePlan`, partitioning, buffering, catalog sink
primitives, and option-universe metadata/readback helpers.

This crate owns deployment policy types and should stay free of live-node wiring.
